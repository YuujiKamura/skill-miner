Below is a list of Claude Code chat conversations. Classify each conversation into the most appropriate domain from the following list. Do NOT use domain names outside this list.

{domain_list}

Return a JSON array. Each element: {{"index": 0, "domain": "Domain Name", "tags": ["tag1"], "confidence": 0.9}}

Conversations:
{formatted_text}
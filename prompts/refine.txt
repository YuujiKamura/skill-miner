Improve the following Claude Code Skill description.

Current description:
{current_description}

Trigger phrases from actual user invocations:
{trigger_contexts}

Improvement guidelines:
- Add synonyms and phrasings that users actually used
- Reinforce the keyword section with real invocation patterns
- Keep the format: "Domain. (pattern summary) Use when user mentions keywords."
- Keep it under 200 characters

Return only the improved description (no JSON, plain text only).